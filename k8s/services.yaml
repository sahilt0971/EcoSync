apiVersion: v1
kind: Service
metadata:
  name: atmosphere
  namespace: ecosync
spec:
  selector:
    app: atmosphere
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: ecosystem
  namespace: ecosync
spec:
  selector:
    app: ecosystem
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: thermal
  namespace: ecosync
spec:
  selector:
    app: thermal
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: ecosync
spec:
  selector:
    app: gateway
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: ecosync
spec:
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5173
  type: ClusterIP
